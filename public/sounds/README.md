Expected filenames for sound effects:

- `coin.mp3` (or `.ogg` / `.wav`)
- `hit.mp3` (or `.ogg` / `.wav`)
- `jump.mp3` (or `.ogg` / `.wav`)
- `crouch.mp3` (or `.ogg` / `.wav`)
- `music.mp3` or `bgm.mp3` for the looping background music
