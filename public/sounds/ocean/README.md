Place Ocean World audio files in this folder with these names:

- `ocean_bg.mp3` for the main background music
- `underwater_ambience.mp3` for the low-volume underwater loop
- `Whale.mp3` for the occasional whale call

`.ogg` and `.wav` alternatives are also supported.
